# Flipper build output
flipper/hotspot-arcade/dist/
flipper/hotspot-arcade/.vscode/
*.fap

# ESP build output
esp32/hotspot-arcade-fw/build/

# Bundled ESP firmware images (build artifacts copied in by tools/build-fap.sh).
# The flash.txt manifest beside them IS committed (source of truth).
flipper/hotspot-arcade/assets/firmware/*.bin

# Web build output is committed on release, but ignore local node cruft
web/node_modules/

# Python
__pycache__/
*.pyc

# ufbt
.ufbt/

# OS
.DS_Store

# Local dev notes (not committed)
CLAUDE.md
